 /*       
for (int i = 0; i < btnMatrix.length; i++){
    for (int j = 0; j < btnMatrix.length; j++){
        int value = Game.getInstance().getCellValue(i, j);
        btnMatrix[i][j].setText(String.valueOf(value));
        if (value < 0){
            btnMatrix[i][j].setBackground(Color.RED);
        }
    }
}
*/